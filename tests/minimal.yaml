esphome:
  name: lilygo-t5-47-plus-test-minimal

packages:
  base: !include base.yaml

display:
  - platform: lilygo_t5_47_plus
    id: epaper
    update_interval: 60s
    lambda: |-
      it.fill(COLOR_OFF);
      it.rectangle(20, 20, 920, 500, COLOR_ON);
